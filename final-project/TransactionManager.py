import DeadlockDector


##Round Robin (which reads one line from each file at a time in turns) 
def round_robin():
    
    return

##Random (which reads files in random order, and reads a random number of lines from each file).
def random_read():

    return

def main():
    dld = DeadlockDector()
    
    
if "__name__" == "__main__":
    main()
